# Project Summary: drawDB (coldrawdb)

> Auto-generated by scan-project skill on 2026-03-10

## Overview

drawDB 是一个免费、简洁、直观的数据库 ER 图编辑器和 SQL 生成器，可直接在浏览器中使用。前端基于 React + Vite 构建，后端正在从纯前端本地存储方案迁移至 Rust (Actix-web + SeaORM + SQLite) 后端持久化方案。当前已完成 V1 后端初版里程碑（Phase 0-3），下一步将进行 Phase 4 Rust Web MVP。

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Frontend Language | JavaScript (JSX) |
| Frontend Framework | React 18 + Vite 6 |
| UI Library | @douyinfe/semi-ui (Semi Design) |
| CSS | Tailwind CSS 4 |
| State Management | React Context + Custom Hooks (13 个 Context) |
| Local Storage | Dexie (IndexedDB) |
| Rich Text Editor | Lexical |
| i18n | react-i18next |
| Backend Language | Rust (Edition 2021) |
| Backend Framework | Actix-web 4 |
| ORM | SeaORM 0.12 (SQLite) |
| ID Generation | rs-snowflake |
| Logging | tracing + tracing-subscriber |
| Config | TOML (config.toml) |
| Containerization | Docker (multi-stage) + docker-compose |
| CI/CD | GitHub Actions |

## Directory Structure

```
coldrawdb/
├── backend/                  # Rust 后端
│   ├── src/
│   │   ├── main.rs           # 入口：HTTP 服务器 + snowflake ID
│   │   ├── init.rs           # 数据库初始化、配置加载、迁移
│   │   ├── diagrams_v1.rs    # v1 Diagrams API (JSON + revision)
│   │   ├── phase3_bridge.rs  # Bridge 配置 + 本地草稿导入
│   │   ├── common/           # 共享 HTTP 响应类型
│   │   ├── error/            # 错误类型定义
│   │   ├── entity/           # SeaORM 实体 + VO + DTO
│   │   ├── repository/       # 数据访问层
│   │   ├── diagrams/         # Legacy Diagrams 路由
│   │   ├── tables/           # Tables 路由 + 内部 API
│   │   ├── todos/            # Todos 路由
│   │   ├── references/       # References (路由未注册)
│   │   ├── notes/            # Notes 内部 API
│   │   └── indices/          # Indices 内部 API
│   ├── migrations/           # SQL 迁移文件
│   ├── config.toml           # 服务器配置
│   ├── init.sql              # 基线建表脚本
│   └── Cargo.toml            # Rust 依赖
├── src/                      # React 前端
│   ├── main.jsx              # 前端入口
│   ├── App.jsx               # 路由定义 + 主题
│   ├── pages/                # 页面组件
│   │   ├── Editor.jsx        # 主编辑器 (11 层 Context Provider)
│   │   ├── LandingPage.jsx   # 营销首页
│   │   ├── Templates.jsx     # 模板浏览
│   │   ├── BugReport.jsx     # Bug 报告
│   │   └── NotFound.jsx      # 404
│   ├── components/           # UI 组件
│   │   ├── Workspace.jsx     # 编辑器主框架
│   │   ├── FloatingControls   # 浮动控件
│   │   └── ...               # Canvas, SidePanel, ControlPanel 等
│   ├── context/              # 13 个 React Context
│   ├── hooks/                # 对应 Context 的自定义 Hooks
│   ├── data/                 # 常量、数据库配置、类型定义
│   ├── utils/                # 工具函数 (SQL 导入/导出、路径计算等)
│   ├── api/                  # HTTP 客户端 (email, gists)
│   ├── i18n/                 # 国际化配置
│   ├── icons/                # 自定义图标组件
│   ├── animations/           # 动画组件 (FadeIn, SlideIn)
│   ├── templates/            # 预置模板数据
│   └── assets/               # 静态资源 (图片、logo)
├── docs/                     # 项目文档
│   ├── MILESTONE_V1_INITIAL.md
│   ├── phase0/               # ERD、OpenAPI、迁移策略、执行计划
│   ├── phase1/               # Phase 1 验证文档
│   ├── phase2/               # Phase 2 验证文档
│   └── phase3/               # Phase 3 验证文档
├── public/                   # 公共静态文件
├── .github/workflows/        # CI/CD 流水线
├── package.json              # 前端依赖
├── vite.config.js            # Vite 配置 (含后端代理)
├── compose.yml               # Docker Compose
├── Dockerfile                # 多阶段 Docker 构建
└── RUST_WEB_REFACTOR_PLAN.md # 重构计划总览
```

## Architecture

### 总体架构

项目采用前后端分离架构，正处于从「纯前端本地存储」向「Rust 后端持久化」的迁移过程中：

- **前端**：React SPA，使用 Context + Hooks 管理状态，Dexie (IndexedDB) 本地持久化，通过 Vite 代理将 `/api` 请求转发到后端。
- **后端**：Actix-web HTTP 服务器，SeaORM 操作 SQLite，支持 legacy 和 v1 两套 API。
- **迁移桥接**：Phase 3 Bridge 提供配置开关和本地草稿导入能力，支持渐进式迁移。

### 数据流

```
Browser (React) → Vite Proxy (/api → :6666) → Actix-web → SeaORM → SQLite
                ↕
           Dexie (IndexedDB) — 本地持久化 (当前主路径)
```

### 后端分层

```
HTTP Routes (main.rs)
  ├── Legacy API (/diagrams, /tables, /todos)
  ├── V1 API (/api/v1/diagrams, /api/v1/bridge)
  ↓
Handler Functions
  ↓
Repository Layer (DiagramRepository)
  ↓
Entity Layer (SeaORM entities + VOs + DTOs)
  ↓
SQLite Database
```

### 前端 Context 嵌套 (Editor 页面)

```
LayoutContext
  → TransformContext
    → UndoRedoContext
      → SelectContext
        → TasksContext
          → AreasContext
            → NotesContext
              → TypesContext
                → EnumsContext
                  → DiagramContext
                    → SaveStateContext
                      → WorkSpace
```

## Key Modules

### Backend

#### `main.rs` — 服务入口
- **Purpose**: 启动 HTTP 服务器，注册路由，初始化全局 Snowflake ID 生成器。
- **Key exports**: `next_id()`, `next_ids(count)`

#### `init.rs` — 初始化与迁移
- **Purpose**: 加载 config.toml 配置，创建/初始化数据库，执行 SQL 迁移。
- **Key exports**: `init()`, `get_config()`, `init_table()`, `apply_migrations()`

#### `diagrams_v1.rs` — V1 Diagrams API
- **Purpose**: 新版 Diagram CRUD，支持 JSON 存储和 revision 冲突检测。
- **Endpoints**: `POST/GET/PUT/DELETE /api/v1/diagrams`, `POST /api/v1/diagrams/import`

#### `phase3_bridge.rs` — Bridge 迁移桥接
- **Purpose**: 提供迁移配置管理和本地草稿导入能力。
- **Endpoints**: `GET/PUT /api/v1/bridge/config`, `POST /api/v1/bridge/import/local`, `GET /api/v1/bridge/import/local/logs`, `POST /api/v1/bridge/import/local/retry/{id}`

#### `entity/` — 数据实体
- **Purpose**: SeaORM 实体定义，包含 11 个核心表实体和对应的 VO/DTO。
- **Entities**: Diagram, DiagramLink, Table, Field, TableLink, Indice, IndiceLink, Reference, Area, Note, Task

#### `repository/` — 数据访问层
- **Purpose**: 封装数据库操作，提供 DiagramRepository。

### Frontend

#### `pages/Editor.jsx` — 主编辑器页面
- **Purpose**: 组合 11 层 Context Provider，渲染 WorkSpace 编辑器。

#### `components/Workspace.jsx` — 编辑器框架
- **Purpose**: 编辑器核心框架，包含 ControlPanel、SidePanel、Canvas，处理加载/保存/自动保存。

#### `context/DiagramContext.jsx` — 图表状态
- **Purpose**: 管理 tables、relationships、database type，提供 CRUD 操作。

#### `data/` — 配置数据
- **Purpose**: 定义常量、数据库类型、数据类型、快捷键、JSON Schema 等。
- **Key files**: `constants.js` (枚举/配置), `databases.js` (6 种数据库元数据), `datatypes.js` (类型定义)

#### `utils/` — 工具函数
- **Purpose**: SQL 导入/导出、SVG 路径计算、图表验证、自动布局等。
- **Key exports**: `exportSQL`, `importSQL`, `calcPath`, `getIssues`, `arrangeTables`

## Data Models

### 核心实体关系

```
Diagram (1) ──── (N) DiagramLink ──── (1) Table
                                 ──── (1) Task
                                 ──── (1) Area
                                 ──── (1) Note
                                 ──── (1) Reference

Table (1) ──── (N) TableLink ──── (1) Field
Table (1) ──── (N) Reference (start/end)

Indice (1) ──── (N) IndiceLink ──── (1) Field
```

### 关键字段
- **ID**: Snowflake 生成的字符串 ID
- **并发控制**: `revision` (乐观锁，409 冲突)
- **软删除**: 所有核心实体支持 `is_deleted`
- **审计**: `updated_at` 时间戳

## API Surface

### Legacy API

| Method | Path | Description |
|--------|------|-------------|
| GET | `/` | 健康检查 |
| GET | `/todos/query/{diagram_id}/{order_field}` | 查询 Todos |
| POST | `/todos/add` | 新增 Todo |
| POST | `/todos/update` | 更新 Todo |
| DELETE | `/todos/delete/{id}` | 删除 Todo |
| GET | `/tables/queryTables/{diagram_id}` | 查询 Tables |
| GET | `/diagrams/queryAll` | 查询所有 Diagrams |
| POST | `/diagrams/add` | 新增 Diagram |
| POST | `/diagrams/update` | 更新 Diagram |
| DELETE | `/diagrams/detele/{id}` | 删除 Diagram (路径有 typo) |

### V1 API

| Method | Path | Description |
|--------|------|-------------|
| POST | `/api/v1/diagrams` | 创建 Diagram (JSON) |
| GET | `/api/v1/diagrams/{id}` | 获取 Diagram |
| PUT | `/api/v1/diagrams/{id}` | 保存 Diagram (revision 检查) |
| DELETE | `/api/v1/diagrams/{id}` | 删除 Diagram |
| POST | `/api/v1/diagrams/import` | 导入 Diagram |
| GET | `/api/v1/bridge/config` | 获取 Bridge 配置 |
| PUT | `/api/v1/bridge/config` | 更新 Bridge 配置 |
| POST | `/api/v1/bridge/import/local` | 导入本地草稿 |
| GET | `/api/v1/bridge/import/local/logs` | 查询导入日志 |
| POST | `/api/v1/bridge/import/local/retry/{id}` | 重试导入 |

### Frontend Routes

| Path | Component | Description |
|------|-----------|-------------|
| `/` | LandingPage | 营销首页 |
| `/editor` | Editor | ER 图编辑器 |
| `/templates` | Templates | 模板浏览 |
| `/bug-report` | BugReport | Bug 报告 |
| `*` | NotFound | 404 |

## Dependencies

### Frontend — Production
| Package | Purpose |
|---------|---------|
| react / react-dom | UI 框架 |
| @douyinfe/semi-ui | UI 组件库 |
| react-router-dom | 路由 |
| dexie / dexie-react-hooks | IndexedDB 本地存储 |
| @lexical/react / lexical | 富文本编辑器 |
| @dnd-kit/* | 拖拽排序 |
| @monaco-editor/react | 代码编辑器 |
| i18next / react-i18next | 国际化 |
| framer-motion | 动画 |
| axios | HTTP 请求 |
| @dbml/core | DBML 解析 |
| node-sql-parser | SQL 解析 |
| jspdf / html-to-image / jszip / file-saver | 导出功能 |
| nanoid | 唯一 ID |

### Frontend — Development
| Package | Purpose |
|---------|---------|
| vite / @vitejs/plugin-react | 构建工具 |
| tailwindcss / @tailwindcss/postcss | CSS |
| eslint / prettier | 代码质量 |

### Backend — Production
| Crate | Purpose |
|-------|---------|
| actix-web | HTTP 框架 |
| sea-orm | ORM (SQLite) |
| serde / serde_json | 序列化 |
| tokio | 异步运行时 |
| rs-snowflake | ID 生成 |
| tracing / tracing-subscriber | 日志 |
| toml | 配置解析 |
| thiserror | 错误处理 |
| once_cell / lazy_static | 全局状态 |
| itertools | 迭代器工具 |

## Development

### Prerequisites
- Node.js 18+
- npm 9+
- Rust stable (通过 `rustup` 安装)
- Cargo
- (可选) SQLite CLI

### Setup

```bash
# 1. 启动后端
cd backend
cargo run
# 默认监听 127.0.0.1:6666

# 2. 启动前端 (新终端)
npm install
npm run dev
# 默认访问 http://localhost:5173
```

### Common Commands

| Command | Description |
|---------|-------------|
| `npm run dev` | 启动前端开发服务器 |
| `npm run build` | 构建前端生产包 |
| `npm run lint` | ESLint 检查 |
| `npm run preview` | 预览生产构建 |
| `cd backend && cargo run` | 启动后端服务 |
| `cd backend && cargo build` | 构建后端 |
| `docker build -t drawdb .` | Docker 构建 |
| `docker run -p 3000:80 drawdb` | Docker 运行 |
| `curl http://127.0.0.1:6666/` | 后端健康检查 |

## Conventions

- **Git 提交**: 现在时祈使语气 (如 "Add feature"、"Fix bug")，中英文混用 (中文在非正式提交中常见)
- **前端代码风格**: Prettier (双引号, 2 空格, trailing comma), ESLint (React recommended)
- **命名约定**: 前端采用 camelCase (变量/函数), PascalCase (组件); 后端 Rust 采用 snake_case
- **架构模式**: 前端 Context + Hooks 分层; 后端 Route → Handler → Repository → Entity 分层
- **状态管理**: 13 个 React Context + 对应 Hooks, Dexie (IndexedDB) 持久化
- **错误处理**: 后端 `thiserror` 定义 `DrawDBError`, 统一 JSON 响应; 前端 Toast 通知
- **ID 生成**: 后端使用 Snowflake ID
- **并发控制**: Revision 乐观锁 (409 冲突)
- **软删除**: 所有核心实体 `is_deleted` 字段
- **测试**: CI 中运行 ESLint + Vite build; 后端暂无自动化测试
- **数据库迁移**: `backend/migrations/*.up.sql` 幂等执行, `schema_migrations` 版本记录
