{"ts":"2026-08-27T15:00:17.401Z","trigger":"merge","old_code":"## [code] 代码实现\n\n- [ ] `frontend-rs/src/editor_core.rs` 新增全局唯一实体 id 生成器：`new_entity_id(prefix) -> String`（`{prefix}-{16位hex}`），wasm 侧随机源 `js_sys::Math::random` + `js_sys::Date::now` 种子 + `AtomicU64` 计数器，非 wasm 侧 `#[cfg]` 走 `std::time` + 原子计数器，保证 host 可测\n- [ ] `frontend-rs/src/editor_panels.rs` 替换建表 id（L6185 `auto-{n}` → `new_entity_id(\"auto\")`）；表默认字段保持 `{table_id}-field-id` 不变（table_id 唯一后自然唯一）\n- [ ] `frontend-rs/src/editor_panels.rs` 替换新增字段 id（L6340 `auto-{n}` → `new_entity_id(\"auto\")`）\n- [ ] `frontend-rs/src/editor_panels.rs` 替换关系 id（L6434 `ref-{n}` → `new_entity_id(\"ref\")`）\n- [ ] `frontend-rs/src/editor_panels.rs` 替换区域/便签 id（`new_default_area` L906 / `new_default_note` L919：`area-{seq}`/`note-{seq}` → `new_entity_id(\"area\")`/`new_entity_id(\"note\")`；seq 参数保留用于命名与层叠落位）\n- [ ] 更新受影响 UT 断言：凡断言 `auto-N` / `ref-N` / `area-N` / `note-N` 具体字面量的用例（含 L8539/L8552 周边）改为「前缀匹配 + 互不相同」断言；确认 enum/type stub（`enum-auto-N`/`type-auto-N`，不持久化）无需改动\n- [ ] 新增回归 UT（含 OpenLogos `verify_reporter::report_pass`，写入 `logos/resources/verify/test-results.jsonl`）：连续生成 1000 个实体 id 互不重复；新格式 id 不被 `parse_num_suffix` 解析（`next_id_from_store` 返回 0，兼容存量加载）\n- [ ] 回归验证：`cd frontend-rs && cargo test --lib` 与 `cargo test --test '*'` 全绿；`cd backend && cargo test` 全绿（确认后端零改动）"}
