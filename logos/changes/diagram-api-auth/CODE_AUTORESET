{"ts":"2026-09-24T06:22:44.642Z","trigger":"merge","old_code":"## [code] 代码实现\n\n- [ ] backend：新增 `COLDRAWDB_DIAGRAMS_AUTH` 配置（默认 off）；diagrams_v1/bridge 端点 JWT 守卫（复用 auth_v1 验证逻辑）；`POST /diagrams/{id}/share` 铸造/轮换；`GET /diagrams/{id}` share_token 豁免读；`init.sql` 加列 + 幂等迁移\n- [ ] mcp-server：启动期检测后端强制模式且无 token → 明确报错退出\n- [ ] `scripts/smoke-local-scripts.sh`：register/login 获取 smoke token 并注入 `Authorization: Bearer`；新增开启模式下 401 断言\n- [ ] 编写/更新对应 UT/ST 测试（含 OpenLogos reporter 写账本）"}
