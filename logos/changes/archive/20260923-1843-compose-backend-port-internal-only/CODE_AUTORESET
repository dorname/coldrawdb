{"ts":"2026-09-23T09:07:15.001Z","trigger":"merge","old_code":"## [code] 代码实现\n\n- [ ] `docker-compose.yml` 移除 `ports: \"3000:3000\"` 宿主映射（保留容器 EXPOSE/内部网络通信，healthcheck 走容器内 localhost:3000 不变）\n- [ ] `compose.release.yml` 同上移除宿主映射\n- [ ] `README.md` 健康检查段补限定语：compose 形态仅 9080 入口；直连 3000 仅适用单容器 `docker run`/开发模式\n- [ ] `scripts/smoke-local-scripts.sh` Stage 8（STABLE-01 执行段）同步增加宿主 3000 无监听断言，与 smoke 用例规格对齐"}
