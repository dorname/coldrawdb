{"ts":"2026-08-23T02:05:50.501Z","trigger":"merge","old_code":"## [code] 代码实现\n\n### A 批 — auth / share / 页面流入口\n\n覆盖：`ST-S03-UI-01`～`05`、`UT-S03-ERR-01`、`ST-S02-SHARE-RO`、`ST-S02-404`、`ST-S02-NO-SHARE`、`ST-S02-SHARE-VS-AUTH`、`UT-S02-ROUTE-01`、`ST-FE-ALIGN-01`、`ST-FE-ALIGN-02`、`ST-PU-22`\n\n- [ ] 对齐默认未登录 `auth-gate`：品牌区、双 tab、字段错误、loading、不枚举用户、无 token 原文；成功进入 `rooms-list-page` 不直达 editor\n- [ ] 对齐 `?share=` 旁路：未登录只读、404 友好、无 share 走 auth、已登录不自动升级可写\n- [ ] 编写/接通上述 UT/ST/e2e，写入 reporter，并从 skip 列表移除本批 ID\n\n### B 批 — rooms / invite\n\n覆盖：`ST-S04-UI-01`～`07`、`ST-PU-23`\n\n- [ ] 对齐 `rooms-list-page`：列表/空状态、创建、刷新、用户菜单；创建/打开进入 `room-editor-page`；`room-badge` 可回 rooms\n- [ ] 对齐 `invite-accept-page`：preview、过期无加入、未登录接受→提示登录并可续接、接受后进入同一 room\n- [ ] 编写/接通上述 UT/ST/e2e，写入 reporter，并从 skip 列表移除本批 ID\n\n### C 批 — room-editor 壳层 / 保存 / 协作\n\n覆盖：`UT-S01-SS-01`、`UT-S01-SS-02`、`ST-S01-SS-01`、`ST-S01-409-SCOPE`、`ST-S01-NO-409-OT`、`ST-S01-409-LOCAL-ONLY`、`ST-S05-UI-01`～`06`、`ST-FE-ALIGN-03`、`ST-FE-ALIGN-04`、`ST-PU-24`\n\n- [ ] 对齐壳层锚点与保存态：`app-bar` / `tool-rail` / `editor-canvas` / `inspector` / `status-bar` / `save-state` / `revision-display`\n- [ ] 对齐协作可见状态：`ws-status` / `ot-rev` / `room-presence` / `reconnect-banner` / Activity；状态来自真实 REST/WS 或明确降级\n- [ ] 对齐 Viewer 只读、协作中禁 S01 409 模态、仅本地编辑后允许 409\n- [ ] 编写/接通上述 UT/ST/e2e，写入 reporter，并从 skip 列表移除本批 ID\n\n### D 批 — IO / 快捷键 / 主题 / 响应式 / 收口\n\n覆盖：`ST-KB-CMD-01`、`ST-KB-ESC-01`、`ST-KB-T-01`、`ST-KB-R-01`、`ST-PC-MENU-01`、`ST-PC-FMT-01`、`ST-PC-INSPECTOR`、`ST-PU-25`、`ST-PU-26`\n\n- [ ] 对齐更多菜单→IO 抽屉、格式预览、Inspector 让位；⌘K / Esc / T / R\n- [ ] 对齐主题切换与 720px：Inspector/IO/模态可达，无横向溢出、可关闭浮层\n- [ ] 回归画布 `GRID_SIZE=20`、关系 4px / rubber-band / 两点 / 确认条（已有能力，本批只补与主原型不一致处）\n- [ ] 编写/接通上述 UT/ST/e2e，写入 reporter；确认 `SPEC_PARITY_SKIP_IDS` 清空或仅余带缺口说明的显式 skip\n- [ ] 按验收清单勾选 §7 已验证项；S01/S02/IO/PU/设计系统回归不退化"}
