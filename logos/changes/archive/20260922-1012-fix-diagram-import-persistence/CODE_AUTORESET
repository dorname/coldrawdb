{"ts":"2026-09-21T10:09:37.360Z","trigger":"merge","old_code":"## [code] 代码实现\n\n- [x] 修复 `backend/src/diagram_persistence.rs` 的 `diagram_from_import_payload`：逐表容错反序列化（坏表跳过并记录）、缺 id 表/字段自动生成 `auto-` 前缀 id、`name` 缺省 `\"imported_diagram\"`\n- [x] 修复 `backend/src/diagram_persistence.rs` 的 `save_diagram`：`name` 为 None 时保持列原值不覆盖\n- [x] 修复 `backend/src/diagrams_v1.rs` 的 `import_diagram_v1`：传播 `persist_import_payload` 错误（失败返回 5xx）、`imported_tables/imported_fields` 按实际持久化数量上报、warnings 携带丢弃明细\n- [x] 修复 `mcp-server/src/service.rs` 的 `import_schema`：payload 前置规范化（补 name 默认值、补 auto- id）\n- [x] 更新 backend 导入 ST 测试（st_s01_02 及新增回归：缺 id 导入、name 兜底、持久化失败路径）\n- [x] 更新 mcp-server 契约测试（c6：import_schema 前置规范化回归用例）"}
