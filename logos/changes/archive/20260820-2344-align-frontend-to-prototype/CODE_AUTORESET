{"ts":"2026-08-19T07:22:22.655Z","trigger":"merge","old_code":"## [code] 代码实现\n\n- [ ] 批次 A：Auth 与 Invite 页面流对齐\n  - [ ] `frontend-rs` 增加显式前端页面状态：auth / rooms / invite / editor。\n  - [ ] 登录/注册成功后进入 `rooms-list-page`，不直接进入编辑器；保留 `?share=` 匿名只读绕过。\n  - [ ] `/invite/{token}` 未登录时也展示 `invite-accept-page` preview 与登录要求，登录后可接受邀请并进入房间。\n  - [ ] 测试覆盖：`UT-FE-PROTO-01`、`UT-FE-PROTO-02`、`ST-FE-PROTO-01`、`ST-FE-PROTO-02`。\n- [ ] 批次 B：Rooms 列表页对齐\n  - [ ] 新增生产 `rooms-list-page`，包含 room cards、空状态、新建房间卡片、刷新、用户菜单与 `btn-create-room`。\n  - [ ] 创建房间成功后进入 `room-editor-page`，AppBar 显示 `room-badge`，并加载成员/presence。\n  - [ ] 保留编辑器内 `room-members-panel` 作为成员/邀请抽屉，不再把它作为唯一房间入口。\n  - [ ] 测试覆盖：`UT-FE-PROTO-03`、`UT-FE-PROTO-04`、`ST-FE-PROTO-03`、`ST-FE-PROTO-04`。\n- [ ] 批次 C：Collab Editor 可见状态与响应式对齐\n  - [ ] 对齐 `room-editor-page` 的 `ws-status`、`ot-rev`、`room-presence`、`activity-feed`、`reconnect-banner`、viewer 只读提示与远端 presence 锚点。\n  - [ ] 强化协作状态从真实 REST/WS 或降级状态进入 UI，不把静态原型模拟事件标为生产同步。\n  - [ ] 对齐 720px 视口下 auth / rooms / editor / members / IO 抽屉关键操作可达，无横向溢出或不可恢复遮挡。\n  - [ ] 测试覆盖：`UT-FE-PROTO-05`、`UT-FE-PROTO-06`、`ST-FE-PROTO-05`、`ST-FE-PROTO-06`、`ST-FE-PROTO-07`。\n- [ ] 批次 D：全链路回归与状态收口\n  - [ ] 回归 S01/S02 保存、分享、409、导入导出、命令面板、设计系统与 ST-PU 主原型测试。\n  - [ ] 更新 `core-implementation-checklist.md` 的完成状态。\n  - [ ] 运行本地可执行测试集合并生成 OpenLogos reporter；用户已授权后运行 `openlogos verify`。\n  - [ ] 测试覆盖：`ST-FE-PROTO-08`、`ST-FE-V2-01`～`ST-FE-V2-04` 回归、ST-PU 回归。"}
